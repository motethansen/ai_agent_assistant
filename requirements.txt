# AI Agent Assistant — dependencies
# Core (always installed)
google-genai>=1.0.0              # Gemini Flash + Pro
groq>=0.11.0                     # Groq Llama 3.3
openai>=1.0.0                    # DeepSeek (OpenAI-compatible API)
rdflib>=7.0.0                    # Knowledge graph (OWL/SPARQL)
icalendar>=5.0.0                 # ICS calendar read/write
rich>=13.0.0                     # Terminal UI
python-frontmatter>=1.1.0        # Obsidian YAML frontmatter
python-dateutil>=2.9.0           # Date parsing
pyyaml>=6.0.0                    # YAML parsing
pytest>=8.0.0                    # Tests
pytest-mock>=3.0.0               # Test mocks
pytest-html>=4.0.0               # HTML test reports (output/test_report.html)

# Optional — uncomment to enable

# Ollama (local LLM — only if OLLAMA_ENABLED=true in .config)
# ollama>=0.3.0

# Google Calendar API (only if GOOGLE_CAL_ENABLED=true in .config)
# google-auth>=2.0.0
# google-auth-httplib2>=0.2.0
# google-api-python-client>=2.0.0

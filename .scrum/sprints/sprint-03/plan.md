# Sprint 03 Plan — AI Agent Assistant

> Generated by: Scrum Master
> Date: 2026-03-15
> Sprint dates: 2026-03-22 → 2026-03-29

---

## Sprint Goal

Polish the assistant: add per-task Ollama model routing, a clean config.example for new users, and an evening review agent that summarises what was completed today.

---

## Sprint Backlog

| Task ID | BLI | Title | Agent | Estimate | Dependencies |
|---------|-----|-------|-------|----------|--------------|
| T03-01 | BLI-020 | Per-task Ollama model routing via `/routing` command | dev-1 | M | None |
| T03-02 | BLI-021 | config.example with sane defaults | dev-2 | S | None |
| T03-03 | BLI-022 | Evening review agent (`/review` + `--evening`) | dev-1 | M | None |

All three tasks are independent and can run in parallel.

---

## Task Acceptance Criteria

---

### T03-01 — Per-task Ollama model routing
**Agent**: dev-1 | **BLI**: BLI-020 | **Estimate**: M

- [x] `ROUTING_CHAT`, `ROUTING_SCHEDULING`, `ROUTING_PARSING` each accept any installed Ollama model name (e.g. `llama3:latest`, `mistral:latest`, `qwen2.5:14b`)
- [x] `/routing` CLI command shows current routing assignments and lets user pick a different Ollama model for each task type
- [x] Changes written to `.config` and take effect immediately (no restart needed)
- [x] `config.template` updated with examples of per-task routing values

---

### T03-02 — config.example with sane defaults
**Agent**: dev-2 | **BLI**: BLI-021 | **Estimate**: S

- [x] `config.example` created at project root — contains only settings needed for Ollama + LogSeq + Obsidian + Google Calendar
- [x] Every setting has a one-line comment explaining it
- [x] Cloud API keys present but commented out for reference
- [x] No OpenClaw references anywhere in the file
- [x] `README.md` updated to reference `config.example` in the Getting Started section

---

### T03-03 — Evening review agent
**Agent**: dev-1 | **BLI**: BLI-022 | **Estimate**: M

- [x] `/review` CLI command shows tasks marked done today across Obsidian (`- [x]`) and LogSeq (`DONE`) journals
- [x] Summary generated by local Ollama LLM and printed in CLI
- [x] `python main.py --evening` triggers the same review from the command line
- [x] Optionally appends the summary to today's LogSeq journal file if `LOGSEQ_DIR` is set
- [x] If no tasks were completed today, prints: "No tasks completed today."

---

## Dependency Graph

```
T03-01 ─── independent
T03-02 ─── independent
T03-03 ─── independent
```

All three can run in parallel on day 1.

---

## Definition of Done

- [x] All acceptance criteria met
- [x] `python main.py` starts cleanly with new features available
- [x] `config.example` committed to repo (not gitignored — it's a template)
- [x] Sprint review prepared
- [x] `progress.md` updated
